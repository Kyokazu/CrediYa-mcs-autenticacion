server:
  port: 8080
spring:
  application:
    name: "msc-autenticacion"
  devtools:
    add-properties: false
  h2:
    console:
      enabled: true
      path: "/h2"
  profiles:
    include: null
management:
  endpoints:
    web:
      exposure:
        include: "health,prometheus"
  endpoint:
    health:
      probes:
        enabled: true
cors:
  allowed-origins: "http://localhost:4200,http://localhost:8080"

springdoc:
  swagger-ui:
    path: "/api/doc/swagger-ui.html"
  api-docs:
    path: "/api/doc/api-docs"

routes:
  paths:
    users: "/api/v1/users"
    usersByMail: "/api/v1/users/{id}"

adapters:
  r2dbc:
    host: "localhost"
    port: 5432
    database: "msc_autenticacion"
    schema: "public"
    username: "admin"
    password: "adminpassword"